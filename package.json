{
  "name": "resumejsongenerator",
  "version": "1.0.0",
  "description": "A tool that uses LLM to generate structured JSON resumes from natural language descriptions.",
  "main": "json.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jsonresume/schema": "^1.2.1"
  }
}
